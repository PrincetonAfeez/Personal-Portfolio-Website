[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python manage.py migrate && python manage.py collectstatic --noinput && gunicorn portfolio_site.wsgi:application --log-file -"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

